apiVersion: v1
kind: Service
metadata:
    name: "k8smarcn10pm-a65f"
    labels:
        app: "k8smarcn10pm-a65f"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "k8smarcn10pm-a65f"