apiVersion: v1
kind: Service
metadata:
    name: "k8smarch10-18de"
    labels:
        app: "k8smarch10-18de"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "k8smarch10-18de"